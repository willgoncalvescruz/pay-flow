class HomeControler {
  var currentPage = 0;

  void setPage(int index) {
    currentPage = index;
  }
}

class HomeControlerVisit {
  var currentPage = 0;

  void setPage(int index) {
    currentPage = index;
  }
}
